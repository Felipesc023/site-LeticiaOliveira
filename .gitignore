# dependencies / build
node_modules
.next
out
*.tsbuildinfo
next-env.d.ts

# env — only .env.example is committed
.env
.env.local
.env*.local

# os
.DS_Store
Thumbs.db

# local tooling (not part of the site)
.graphify/
.agents/
.claude/
strix_runs/
